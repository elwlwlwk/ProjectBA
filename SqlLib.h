#include <mysql.h>
#include <stdio.h>

/*
int InitSql(MYSQL* mysql){
	mysql_init(mysql);

	if(!mysql_real_connect(mysql, "localhost", "root", "neostage", 
"br", 0, (char*)NULL, 0)){
		printf("sql connect error");
		return 1;
	}

	return 0;
}
*/
